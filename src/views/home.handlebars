<h1 class="title-main" >Meme's House</h1>
<div class="container-items">
    {{#each productList}}
        <div class="item">
            <figure>
                <img src={{thumbnails}} alt={{title}}>
            </figure>
            <div class="info-product">
                <h2>{{title}}</h2>
                <p>${{price}}</p>
                <button class="btn-add-cart">Añadir al carrito</button>
            </div>
        </div>
    {{/each}}
    </div>